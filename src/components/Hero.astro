---
import Reveal from "./Reveal.astro";
import StatsBar from "./StatsBar.astro";

const eyebrows = ["Engineering Leader", "Solution Architect", "AI-Native"];
---

<section class="pt-2 md:pt-4">
  <Reveal>
    <div
      class="flex flex-wrap items-center gap-x-3 gap-y-1 font-mono text-[11px] uppercase tracking-[0.18em] text-accent"
    >
      {
        eyebrows.map((e, i) => (
          <>
            {i > 0 && <span class="text-muted">·</span>}
            <span>{e}</span>
          </>
        ))
      }
    </div>
  </Reveal>

  <Reveal delay={80}>
    <h1
      class="mt-7 font-display tracking-tight text-ink"
      style="font-size: clamp(2.5rem, 5.4vw, 4.5rem); line-height: 1.02;"
    >
      I lead engineering teams and <em class="italic text-accent"
        >design the systems</em
      > they build.
    </h1>
  </Reveal>

  <Reveal delay={160}>
    <p
      class="mt-7 max-w-[58ch] text-[17px] leading-relaxed text-ink-soft"
    >
      Twenty-four years of full-lifecycle engineering. Twelve leading the people
      who do it. Today, leading Wealth & Asset Management Engineering at Charles
      Schwab — and looking for what's next.
    </p>
  </Reveal>

  <Reveal delay={220} class="mt-10">
    <StatsBar />
  </Reveal>
</section>
